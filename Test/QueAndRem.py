numerator = int(input("Enter dividend: "))
denominator = int(input("Enter divisor: "))

quotient = numerator // denominator
remander = numerator % denominator

print(f"Quotient = {quotient}")
print(f"Remainder = {remander}")